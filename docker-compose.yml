services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: local
      PORT: 8080
      DB_CONNECTION_URL: postgresql://neondb_owner:npg_rkwf2MUbm0HG@ep-lucky-cake-a1165spu-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      JWT_SECRET: demo_secret
      JWT_EXPIRY: 1y

  frontend:
    image: faridmurshed9/jt-tech-analytica-fe
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_BASE_URL: http://localhost:8080
    depends_on:
      - backend